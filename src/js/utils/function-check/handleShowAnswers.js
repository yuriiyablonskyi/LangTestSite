export default function formatNumber(flag) {
  const btnToAnswers = document.getElementById('show-answer')
  const btnFinish = document.getElementById('finish-btn')
  if (!flag) return
  btnFinish.disabled = true
  btnToAnswers.addEventListener('click', () => {

  })

}
